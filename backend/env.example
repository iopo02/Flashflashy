PORT=5000
MONGODB_URI=your_database_connection_string
NODE_ENV=development

# For MongoDB Atlas, use format:
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/flashflashy?retryWrites=true&w=majority
# 
# For local MongoDB, use:
# MONGODB_URI=mongodb://localhost:27017/flashflashy
#
# Note: DATABASE_URI is also supported as an alias for MONGODB_URI

# Admin User ID (optional - can also be set in config/admin.js)
# ADMIN_USER_ID=your_admin_user_id_here

